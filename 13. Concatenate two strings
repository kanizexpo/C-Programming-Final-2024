// Concatenate two strings.
#include<stdio.h>
#include<string.h>
int main()
{
    char a[200],b[100];
    scanf("%s %s", a,b);
    int i,size=0;
    size= strlen(a);
    for(i=0; i<=strlen(b);i++)
    {
        a[size]=b[i];
        size++;
    }
    printf("%s",a);
}
