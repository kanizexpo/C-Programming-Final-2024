#include <stdio.h>
int main() 
{
    int a[50], n,i, sum=0;
    printf("How many numbers you want to input?\n");
    scanf("%d", &n);
    for(i=1; i<=n; i++)
    { scanf("%d", &a[i]);
        sum = sum + a[i];
    }
    printf("Sum= %d", sum);
}
