#include <stdio.h>
int main() 
{
    int a[50], n,i;
    printf("How many numbers you want to input?\n");
    scanf("%d", &n);
    for(i=1; i<=n; i++)
    { scanf("%d", &a[i]);}
    printf("Display values\n");
    
    for(i=n; i>0; i--)
    { printf("%d\n", a[i]);}
}
