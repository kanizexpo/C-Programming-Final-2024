#include <stdio.h>
#include <string.h>

int main() {
    int l, count = 0, i;
    scanf("%d", &l);
    char str[l + 1];
    scanf("%s", str);
    for (i = 0; str[i] != '\0'; i++) {
        count++;
    }
    printf("Length = %d", count);
    return 0;
}
