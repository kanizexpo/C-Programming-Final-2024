// print all natural numbers from n to m using recursion
#include <stdio.h>
void num(int a,int b)
{ if(a>b)
  { return; }
  printf("%d \n", a);
  num( a+1, b);
}
int main() 
{
    int n,m;
    scanf("%d %d", &n, &m);
    num(n,m);
}
